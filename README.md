An approach for a test k8s task, using the `kind` package to set up a testing environment on a single machine. 

## Testing:
```bash
# install dependencies: `kind`, `kubectl`, `docker`
./deploy.sh
```

To bring down:
```bash
./deploy.sh -d
```
